name: demo 
on:
  push:
    branches:
      - demo 

jobs:
  version-num:
    runs-on: ubuntu-latest
    name: version-lasest 
    steps:
      - name: Install Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' 
      - name: demo 
        run: python rpappalax/testapp2/demo.py
